{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
        <ul>
            {% for p in post %}
                <li>
                    <h5><b>{{ p.title }}</b></h5>
                </li>

                <li>
                    <b> {{ p.text }}</b>
                </li>

                <li>
                    <h6><b>{{ p.autor }}</b></h6>
                </li>

                <form action="/post/{{p.id}}" >
                    <button type="submit" class="btn-small">Delete</button>
                </form>
                <hr>

            {% endfor %}
        </ul>
        </div>
    </div>
{% endblock %}
